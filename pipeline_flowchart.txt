News Credibility Classification — Pipeline Flowchart
=====================================================

[RAW CSV DATASET]
      |
      v
[DATA LOADING & VALIDATION]
  - Read CSV with pandas
  - Verify required columns (author, title, label)
  - Impute missing values with empty strings
      |
      v
[INPUT CONSTRUCTION]
  - Concatenate author + title -> composite text field
      |
      v
[TEXT NORMALISATION]
  - Strip non-alphabetic characters (regex)
  - Lowercase all tokens
  - Remove English stopwords (NLTK)
  - Apply Porter stemming (NLTK)
      |
      v
[TRAIN / TEST SPLIT]
  - Stratified 80/20 split (seed=42)
      |
      v
[TF-IDF VECTORISATION]
  - Fit vectoriser on TRAINING split only
  - Transform both splits -> sparse feature matrices
      |
      v
[MODEL TRAINING]  (three parallel branches)
  |           |              |
  v           v              v
[Logistic  [Naive        [Random
Regression] Bayes]        Forest]
  |           |              |
  v           v              v
[PREDICTIONS on held-out TEST SET]
      |
      v
[EVALUATION]
  - Accuracy, Precision, Recall, F1-Score
  - Classification report (per-class)
  - Confusion matrix (optional PNG output)
      |
      v
[COMPARATIVE RESULTS SUMMARY TABLE]
